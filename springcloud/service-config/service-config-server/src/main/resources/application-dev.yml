spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/qq1623299667/springcloudconfig.git
          searchPaths: '{application}'
      label: master
      profile: aaa
server:
  port: 8888
